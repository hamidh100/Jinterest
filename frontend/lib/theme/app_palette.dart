import 'package:flutter/material.dart';

class AppPalette {
  AppPalette._();

  // Dark-mode palette: change only these four colors to test a new palette.
  static const background = Color(0xFF0C0C0C);
  static const surface = Color(0xFF9B3922);
  static const surfaceHighlight = Color(0xFF481E14);
  static const accent = Color(0xFFF14F29);
}
