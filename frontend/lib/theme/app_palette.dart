import 'package:flutter/material.dart';

class AppPalette {
  AppPalette._();

  // Dark-mode palette: change only these four colors to test a new palette.
  static const background = Color(0xFF211832);
  static const surface = Color(0xFF5C3E94);
  static const surfaceHighlight = Color(0xFFB73A3A);
  static const accent = Color(0xFFF25912);
}
