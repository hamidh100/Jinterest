import 'package:flutter/material.dart';

class AppPalette {
  AppPalette._();

  // Dark-mode palette: change only these four colors to test a new palette.
  static const background = Color(0xFF000000);
  static const surface = Color(0xFF0B192C);
  static const surfaceHighlight = Color(0xFF1E3E62);
  static const accent = Color(0xFFFF6500);
}
